# frozen_string_literal: true

source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in mini_api.gemspec.
gemspec

gem 'puma'

gem 'sqlite3'

gem 'debug', '>= 1.0.0'
gem 'rubocop'
gem 'rubocop-minitest'
gem 'rubocop-performance'
gem 'simplecov'

gem 'kaminari'
